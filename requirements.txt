# ── Devnagri_LLM pinned dependencies ────────────────────────────────────────
# Installed via: pip install -r requirements.txt
# Pinned so Kaggle's preinstalled defaults never get picked up and silently
# conflict with what the pipeline code expects (e.g. torchao 0.10.0 default
# vs peft's LoRA dispatcher requiring torchao>=0.16.0).
#
# torch/torchvision/torchaudio are intentionally NOT pinned here — Kaggle's
# base image ships a CUDA-matched torch build, and reinstalling a different
# torch version from PyPI risks losing GPU support entirely.

# --- fixes the Stage 3 crash (PeftModel.from_pretrained / torchao check) ---
torchao==0.16.0
peft==0.15.2

# --- core HF / training stack ---
accelerate==1.1.1
# >=0.45.5 required: older bitsandbytes imports triton.ops.matmul_perf_model,
# which newer Triton (already on Kaggle's image) removed -> ModuleNotFoundError.
bitsandbytes==0.48.1
huggingface_hub==0.26.2
tokenizers==0.20.3
sentencepiece==0.2.0
# >=5.26 required: Kaggle's preinstalled TensorFlow needs
# google.protobuf.runtime_version, which only exists in protobuf 5.26+.
# Pinning to 4.x breaks TensorFlow's import chain, which transformers
# pulls in lazily and that takes peft down with it.
protobuf==5.29.3

# --- tokenizer / NLP utilities ---
beautifulsoup4==4.12.3
lxml==5.3.0
wikiextractor==3.0.8
indic-nlp-library==0.92
regex==2024.11.6
sacremoses==0.1.1
range-coder==1.1

# --- data / reporting ---
numpy==1.26.4
pandas==2.2.3
matplotlib==3.9.2
seaborn==0.13.2
tabulate==0.9.0
loguru==0.7.2
python-docx==1.1.2
zstandard==0.23.0

# --- progress bars ---
tqdm==4.66.5
